# Test dependencies. Install with:
#   pip install -r requirements-dev.txt
#
# Run tests:
#   pytest                       # uses pytest.ini's --cov + fail_under gate
#   pytest --no-cov              # opt out of coverage gate when iterating
-r requirements.txt
pytest>=8.0.0
# Coverage gate (configured in pytest.ini). 70% is the floor; the repo
# is currently around 90%, but the gate stops a regression below 70%
# from sliding past CI silently.
pytest-cov>=4.0
# Required by fastapi.testclient.TestClient (Starlette's HTTP test
# transport). Used by tests that exercise full route registration —
# URL patterns, HTTP method routing, response serialization — beyond
# what calling the handler function directly can cover.
httpx>=0.27.0
