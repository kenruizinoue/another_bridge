# Another Coder — Tasks

Tasks tracked alongside the architecture docs. Mark items `[x]` as done.

Phase 1 of the refactor (extract services layer) landed in `8427d17` on
`feat/refactor-services`. Items below are the remaining Phase 2–4 work
from the original audit plus the test plan that lives outside Phase 1.

---

## Sprint 1 (2026-apr-29 ~ ) — Refactor follow-ups

- [x] **Phase 1 — Extract services layer** — `services/git_service.py`, `services/github_service.py`, `services/claude_runner.py`, `services/repo_context.py`, `services/request.py`. Routers shrink ~580 lines net; `routers/implementation.py` 614 → 478, `routers/planning.py` 406 → 371, `routers/github.py` 148 → 77, `routers/repos.py` 186 → 159. All 102 tests pass; three test fixtures rewritten to patch the new `claude_runner` seams. `resolve_pr_repo_slug` deliberately stays defined in `routers/implementation.py` so its `_git_origin_url` lookup binds to the local module name (the patch target tests in `test_implementation_pr_creation` rely on).

- [x] **Phase 2 — State & sessions** — all three sub-items complete. The single Phase 2 line is preserved here as a compact summary; full per-item context lives in the bullets below.

- [x] **Phase 2 (2) — Delete `routers/legacy.py`** — confirmed zero references to `/implementTicket` / `/verifyApiKey` from `another_agent_backend`. Dropped the file (`PREVIOUS_RESULT` global, demo handler, unauthed route), the `app.include_router(legacy.router)` mount, the now-dead `TARGET_DIRECTORY` / `ASK_PROFILE_ID` / `ANOTHER_LOGIC_API_KEY` / `ANOTHER_LOGIC_BASE_URL` / `PROFILE_FILE` from `config.py`, the legacy log fields in `main.py`'s `on_startup`, and the orphaned `profiles/` directory (was only read via `PROFILE_FILE`). 124/124 tests still pass.

- [x] **Phase 2 (1) — SQLite-backed session store** — replaced the in-memory `_session_map: dict[str, str]` + `threading.Lock()` in `routers/chat.py` with `services/session_store.SessionStore`. SQLite (WAL mode for crash safety; single connection + `threading.Lock` because uvicorn worker threads need `check_same_thread=False`) keyed on `(conversation_id PRIMARY KEY, session_id, last_seen_at)`. DB path resolved from `ANOTHER_CODER_SESSION_DB_PATH` env var, default `~/.another_coder/sessions.db`; tests use `:memory:` via `tests/conftest.py`. Restart-survival is now the contract — a mid-day `uvicorn` restart no longer drops every active Claude Code session. `prune_older_than(seconds)` is also wired up so the future TTL reaper has its hook ready. 7 dedicated unit tests (`tests/test_session_store.py`) plus the existing chat-flow tests retargeted to the store. 141 → 148 total tests, all passing.

- [x] **Phase 2 (3) — TTL reaper for `JobManager` and `SessionStore`** — `services/reaper.py` runs a daemon thread that calls `JobManager.prune_finished_older_than(3600)` and `SessionStore.prune_older_than(7 * 86400)` every 10 minutes. Defaults tunable via `ANOTHER_CODER_JOB_TTL_SECONDS` / `ANOTHER_CODER_SESSION_TTL_SECONDS` / `ANOTHER_CODER_REAPER_INTERVAL_SECONDS` (with safe fallback when an env var is unparseable so a typo can't silently disable the reaper). Lifecycle owned by FastAPI's `lifespan` in `main.py` — `reaper.start()` on startup, `reaper.stop()` after the yield. `Reaper.prune_once()` is exposed for synchronous test driving + idempotent `start()` / clean `stop()` semantics so a duplicate lifespan invocation can't spawn two daemons. Errors from either prune are caught and logged so a transient DB lock or job-dict contention can't take down the reaper for the rest of the process lifetime. 15 dedicated tests (`tests/test_reaper.py`) covering: prune-finished-older-than (running jobs never touched), Reaper.prune_once driving both prunes, error swallowing, thread lifecycle, env-tuned construction. 148 → 163 total tests, all passing.

- [ ] **Pydantic models for every webhook payload + size limits** — `routers/implementation.py:537-565` and `routers/planning.py:341-379` both hand-validate the body with `_extract_args` + isinstance checks + manual `int()` coercion. Replace with a `PlanningRequest` / `ImplementationRequest` `BaseModel` (declared in each router file or a new `routers/_schemas.py`), let FastAPI handle 422s, and add `max_length` constraints on `ticket_body` and `plan` (current `_run_implementation_job` truncates `plan` to 4000 chars via `plan_excerpt`, but the inbound payload has no cap — a 1MB plan body would silently slow the planning prompt build). Picks up some of `_extract_args`'s remaining duplication too.

- [ ] **Phase 3 — Config & DI** — two sub-items, both unblock testability. (1) **Move hardcoded paths from `config.py` to `.env` / `pydantic-settings`** — `config.py:19` hardcodes `~/Desktop/another_logic_backend` (only used by `routers/legacy.py`, will go away with that file), `config.py:20` builds `PROFILE_FILE` from `__file__` (works but ties the import path to disk layout), `config.py:23` hardcodes `http://localhost:3000` for `ANOTHER_LOGIC_BASE_URL`. Replace with a `Settings(BaseSettings)` class so tests can override via env + production deploys can move off localhost without code changes. (2) **Pass `job_manager` and service instances via FastAPI `Depends()`** — `jobs.py:252` is a module-level singleton imported everywhere (`from jobs import job_manager`); same for the future SQLite session store. Switch to `Depends(get_job_manager)` so test fixtures can inject a fresh JobManager per test instead of cleaning `_jobs` after each (see the `_clean_jobs` autouse fixtures in `test_chat_polling.py:40` and `test_chat_repo_path_wiring.py:40`).

- [ ] **Phase 4 — Hygiene** — three sub-items, lower priority than Phase 2/3. (1) **Structured error types** — `PlanningError`, `GitPushError`, `PRCreateError` in `services/errors.py`, each with an explicit HTTP status code; raise instead of returning `{"error": ...}` dicts. The runners currently call `job_manager.mark_failed(job_id, msg)` with a string; an error type carries the failure shape (was it spawn / timeout / cancel / git push / PR API) so the platform's poller can render different UI per failure mode. (2) **Don't attempt PR creation if push failed** — `routers/implementation.py:300-400`-ish — actually we do bail at `routers/implementation.py:269-271` (`if push_result.returncode != 0: fail(...); return`), so this might already be done from the audit's perspective. Verify and close. (3) **Per-request timeout overrides** — `IMPLEMENTATION_TIMEOUT_SECONDS = 1800` and `PLANNING_TIMEOUT_SECONDS = 1800` are module-level constants; accept an optional `timeout_seconds` arg in the request body (cap at 1800) so the platform can request shorter budgets for trivial tickets without forking the timeout config.

## Sprint 2 — Test plan (was Phase 1's "unblock tests" companion, now standalone)

- [ ] **`tests/conftest.py` shared fixtures** — three things should be deduplicated across the existing tests: (1) the `tmp git repo` fixture (init + commit + add origin pointing at a local bare repo) — currently every test that needs git on disk inlines its own setup or skips it entirely, (2) a `mocked Popen` fixture that returns a configurable `_StubProc` — `test_chat_polling.py:144-164`, `test_chat_repo_path_wiring.py:61-81`, and `test_planning_payload.py:_StubProcCompleted` (now via the shim) all redefine variants, (3) a `fake GitHub API client` (built on `respx` or `responses`) so the upcoming `test_github_service.py` tests can assert on URL/headers/body without real HTTP. Saves ~80 lines across the future test files.

- [ ] **`tests/test_github_service.py`** — covers nothing currently; needs full coverage of `services/github_service.py`. (1) `search_issues(repo, label, state)` happy path + non-OK response → error string + filters out `pull_request` entries; (2) `get_issue(repo, issue_number)` happy path with comments + non-OK → error + 422-when-pull-request → "is a pull request" error; (3) `create_pr(slug, title, body, head, base)` builds the right URL + JSON body + auth header (no real HTTP — mock the requests session). The PR-creation enrichment (`format_pr_creation_error`) is already covered in `test_implementation_pr_creation.py:130-253`; don't duplicate.

- [ ] **`tests/test_git_service.py`** — exercise the deduped git helpers. `_run_git`, `_branch_exists_locally`, `_branch_exists_on_remote`, `_detect_default_branch`, `_resolve_base_branch`, `_git_origin_url`, `_parse_owner_repo`. The owner_repo regex (`_GITHUB_URL_RE` in `services/git_service.py:21`) is partially covered indirectly by `test_implementation_pr_creation.py:35-63` (HTTPS, SSH, no .git suffix); pull those into a direct `test_git_service.py::TestParseOwnerRepo` and add malformed inputs. Use the `tmp git repo` fixture from `conftest.py` for the runners — `_run_git(["status", "--porcelain"], repo)` etc.

- [ ] **`tests/test_implementation_flow.py`** — full webhook integration. Currently `test_implementation_pr_creation.py` only covers the PR-creation helpers in isolation. This file should drive the whole `_run_implementation_job` from input to mark_done: HTTP request → job dispatch → poll status → assert final payload shape. Mock `claude_runner.run_blocking` (already a clean seam after Phase 1) + `subprocess.run` for `git push` + `github_service.create_pr`. Cover: (a) happy path (clean tree → branch → claude succeeds → push OK → PR created → status `done`), (b) dirty working tree rejected at `routers/implementation.py:217-222`, (c) branch already exists locally (`:231-233`) and on origin (`:234-236`), (d) base branch resolution failure, (e) claude timeout, (f) claude cancellation, (g) push failure, (h) PR creation 422 with the head-invalid enrichment text.

- [ ] **`tests/test_planning_flow.py`** — same shape as the implementation flow test but for planning. The `TARGET_REPO_MISMATCH` path through `_run_planning_job` is currently locked down by `test_planning_payload.py:TestTargetRepoMismatchSuppression` (3 cases) but only at the payload level — no end-to-end webhook test. Drive this one through `TestClient` like the chat tests do, mock `claude_runner.run_blocking`, assert `__context__` is suppressed when the marker is present and emitted otherwise.

- [ ] **`tests/test_chat_session_lifecycle.py`** — currently `test_chat_polling.py` covers polling primitives + kickoff event; `test_chat_repo_path_wiring.py` covers repo_path normalization. Neither covers session-id round-tripping: first call captures `session_id` from system init → stored in `_session_map` → second call resumes via `--resume`. Three cases: (a) first call on a fresh `conversation_id` does NOT pass `--resume`, captures + stores the `session_id`, (b) second call on the same `conversation_id` DOES pass `--resume <captured_id>`, (c) failed first run (claude exits non-zero) does NOT store the `session_id` so the next attempt starts fresh. Plus a reaper test once the TTL reaper from Phase 2 lands.

- [ ] **`tests/test_repo_path_traversal.py`** — `validate_repo_path` in `routers/repos.py:74-110` already rejects `..` traversal (via `os.path.commonpath`) and absolute paths outside `WORKSPACE_ROOT`, but no test explicitly hits the symlink-escape case. Create a tmpdir layout: `workspace/legit_repo` (with `.git/`) + `outside_workspace/secret_repo` (with `.git/`) + `workspace/escape_link` symlinking to `../outside_workspace/secret_repo`. Assert: `validate_repo_path("workspace/legit_repo")` passes, `validate_repo_path("workspace/escape_link")` returns `(None, "outside the workspace root")`. The `os.path.realpath` call at `routers/repos.py:84` resolves the symlink before the commonpath check, so this should already be guarded — the test is the regression lock.

- [ ] **`tests/test_subprocess_cancel_race.py`** — SIGTERM → SIGKILL escalation under load. `JobManager._terminate_process` in `jobs.py:181-217` sends SIGTERM, waits `SIGKILL_GRACE_SECONDS` (default 3.0, overridden to 0.05 in `test_jobs_cancel.py:60`), then SIGKILL. The existing `test_jobs_cancel.py` covers the single-process path. Missing: (a) a process that ignores SIGTERM (sleep 60s in a `bash -c 'trap "" TERM; sleep 60'` subshell) → confirm it gets SIGKILL'd within `SIGKILL_GRACE_SECONDS + slack`, (b) cancel mid-spawn race (call `JobManager.cancel(job_id)` before the runner's `attach_process` lands — `JobManager.attach_process:90-102` already handles this via `already_cancelled`; lock it down), (c) cancel after `detach_process` is a no-op (already covered by the comment in `jobs.py:104`, but no test asserts it).

- [ ] **Tooling: `pytest-cov` with `fail_under=70`** — add to `requirements-dev.txt` + `pytest.ini`. Current 102 tests pass, but coverage is uneven: `services/repo_context.py` (37 lines) and `services/request.py` (11 lines) probably hit 100%, but `routers/implementation.py:_run_implementation_job` (~250 lines) is only partially covered through the PR-creation helpers. Failing under 70% will surface the gap concretely.

- [ ] **Tooling: `respx` or `responses` for GitHub mocking** — pick one and use it in `test_github_service.py` instead of hand-rolled `requests.get` patches. `respx` is async-first (works if we go `httpx.AsyncClient` later), `responses` is sync-only (matches today's `requests` usage). Default to `responses` until the codebase moves off `requests`.

## Sprint 3 — v2 ideas (not started)

- [ ] **Hybrid execution mode (cloud Claude + local subprocess)** — deferred per `~/.claude/projects/.../memory/project_v2_hybrid_mode.md`. Don't start until v1 platform features stabilize.
- [ ] **Persistent JobManager across restarts** — currently in-memory only (`jobs.py:251`). SQLite would be enough; the platform polls `/jobs/<id>/status` so a coder restart mid-run currently 404s the next poll.
